PLACE

<%= form_with(model: @profile, url: wizard_path, local: true) do |f| %>
  <%= f.label :city %>
  <%= f.text_field :city %>
  <%= @profile.errors[:city].join(', ').capitalize %>
  
  <%= f.submit 'Siguiente' %>
<% end %>
